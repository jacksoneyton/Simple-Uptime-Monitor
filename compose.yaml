services:
  uptime-monitor:
    build: .
    container_name: simple-uptime-monitor
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    ports:
      - "5000:5000"
    environment:
      - TZ=America/New_York
